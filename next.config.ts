HEAD
module.exports = {
  experimental: {
    appDir: false
  },
};

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
b273c4 (Initial commit from Create Next App)
